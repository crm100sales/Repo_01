trigger ExpensesTrigger on Expenses_Card__c (before insert, before update, before delete, 
 after insert, after update, after delete, after undelete) {

     /*
     //This trigger will call Apex class
     //new ExpensesHandler().handleTrigger(trigger.new, trigger.old);    
    */
}